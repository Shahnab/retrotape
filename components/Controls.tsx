import React from 'react';

// Note: Most controls are now directly on the Walkman component for the visual integration.
// This component is kept if we need external overlay controls in the future, 
// but currently returns null to clean up the UI.

const Controls: React.FC = () => {
  return null;
};

export default Controls;